<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.albion.Mapper.BattleMapper">

    <insert id="insertBattle" parameterType="com.web.albion.dto.BattlesDto">
        INSERT INTO battlelog_info(battle_id,
                                   match_type,
                                   battle_time)
        VALUES (#{battle_id},
                #{match_type},
                #{battle_time})
    </insert>

    <select id="checkBattleExists" parameterType="int" resultType="int">
        SELECT COUNT(*) FROM battlelog_info WHERE battle_id = #{battle_id}
    </select>

    <select id="getBattleByBattleId" parameterType="int" resultType="com.web.albion.dto.BattlesDto">
        SELECT * FROM battlelog_info WHERE battle_id = #{battle_id}
    </select>

    <select id="getBattles" resultType="com.web.albion.dto.BattlesDto" parameterType="int">
        SELECT *
        FROM battlelog_info
        ORDER BY battle_time DESC
        LIMIT 3 OFFSET #{offset}
    </select>

    <select id="getBattlesDuo" resultType="com.web.albion.dto.BattlesDto" parameterType="int">
        SELECT *
        FROM battlelog_info
        WHERE match_type = '22'
        ORDER BY battle_time DESC
        LIMIT 3 OFFSET #{offset}
    </select>

    <select id="getBattlesFive" resultType="com.web.albion.dto.BattlesDto" parameterType="int">
        SELECT *
        FROM battlelog_info
        WHERE match_type = '55'
        ORDER BY battle_time DESC
        LIMIT 3 OFFSET #{offset}
    </select>

    <select id="getBattlesTen" resultType="com.web.albion.dto.BattlesDto" parameterType="int">
        SELECT *
        FROM battlelog_info
        WHERE match_type = '1010'
        ORDER BY battle_time DESC
        LIMIT 3 OFFSET #{offset}
    </select>

</mapper>